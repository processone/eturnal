name: Update DockerHub README

on:
  push:
    paths:
      - docker-k8s/README.md
      - .github/workflows/update-dockerhub-readme.yml
env:
  README: docker-k8s/README.md
  DOCKERHUB_REPO: eturnal/eturnal

jobs:
  dockerhub:
    name: Update DockerHub README
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Prepare ${{ env.README }}
        run: |
          sed -i 's|ghcr.io/processone/eturnal|docker.io/${{ env.DOCKERHUB_REPO }}|g' ${{ env.README }}
          sed -i 's|GitHub Packages|DockerHub|g' ${{ env.README }}
          sed -i 's|https://github.com/processone/eturnal/pkgs/container/eturnal|https://hub.docker.com/r/${{ env.DOCKERHUB_REPO }}|g' ${{ env.README }}
      - name: Docker Hub Description
        uses: peter-evans/dockerhub-description@v3
        if: |
          github.event_name != 'pull_request'
          && github.repository_owner == 'sando38'
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.DOCKERHUB_REPO }}
          readme-filepath: ./${{ env.README }}
          short-description: STUN/TURN standalone server
